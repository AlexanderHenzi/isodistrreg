% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modeling.R
\name{isotonic_regression}
\alias{isotonic_regression}
\title{Isotonic mean regression}
\usage{
isotonic_regression(y, X = NULL, weights = NULL, decreasing = FALSE)
}
\arguments{
\item{y}{numeric vector of response values.}

\item{X}{numeric vector of covariate values, or \code{NULL} if responses are
pre-sorted.}

\item{weights}{numeric vector of finite, non-negative weights, at least one
of which must be positive, or \code{NULL} for equal weights.}

\item{decreasing}{whether the fit is decreasing in the covariate (default
\code{FALSE} is increasing, \code{TRUE} is decreasing).}
}
\value{
Numeric vector of isotonic fitted means, one per observation.
}
\description{
Computes isotonic mean regression for numeric responses. When
  covariates are supplied they determine the ordering; when omitted the
  responses are assumed pre-sorted (regression on the index). When weights
  are omitted every observation receives weight 1.
}
\examples{
isotonic_regression(c(2, 3, 1, 4, 5), X = 1:5)
isotonic_regression(c(3, 2, 4, 1), X = 1:4, weights = c(1, 2, 1, 1))
isotonic_regression(sort(c(3, 1, 2, 5)))
isotonic_regression(sort(c(2, 1, 3)), weights = c(1, 2, 1))

}
